apiVersion: v1
kind: ConfigMap

metadata:
  name: cloudstream-config
  namespace: cloudstream

data:
  DATABASE_HOST: postgres
  DATABASE_PORT: "5432"
  DATABASE_NAME: cloudstream

  MINIO_ENDPOINT: minio:9000
  MINIO_BUCKET: cloudstream

  ACCESS_TOKEN_EXPIRE_MINUTES: "30"
  ALGORITHM: HS256